import styled, { css } from "styled-components";

export const MainPageRoot = styled.div(
    ({ theme }) => css`
        /* flex: 1;
        display: flex;
        box-sizing: border-box; */
    `
);
